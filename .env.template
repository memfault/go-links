# ===========================================================================
# Environment Variables for Trotto + oauth2-proxy Deployment
# Copy this to .env and fill in the values
# ===========================================================================

# ---- Deployment Configuration ----
DOMAIN=go.example.com

# ---- OAuth Provider Configuration ----
OAUTH2_CLIENT_ID=
OAUTH2_CLIENT_SECRET=

# Microsoft Entra ID (Azure AD) Tenant ID
# Find at: Azure Portal > Microsoft Entra ID > Overview
OAUTH2_TENANT_ID=

# Should match the domain configured above
OAUTH2_REDIRECT_URL=https://go.example.com/oauth2/callback
OAUTH2_EMAIL_DOMAIN=example.com
OAUTH2_PROVIDER_DISPLAY_NAME=Microsoft

# ---- GENERATED SECRETS ----
# Generate with: openssl rand -base64 32
OAUTH2_COOKIE_SECRET=

# Generate with: openssl rand -base64 32
SESSIONS_SECRET=

# ---- TROTTO CONFIGURATION ----
# Base64-encoded Trotto configuration JSON
# Generate with: ./server/dump_config_as_base64.sh
# CONFIG_BASE64=
